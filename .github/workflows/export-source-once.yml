name: Export source once

on:
  push:
    branches: ["agent/refactor-dual-end-architecture"]

permissions:
  contents: read

jobs:
  export:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Source export completed and disabled"
