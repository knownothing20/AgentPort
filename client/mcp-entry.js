// Explicit client-side MCP entrypoint.
// Compatibility: the existing root index.js remains the implementation source
// until its handlers are migrated into packages/client-core incrementally.
import "../index.js";
